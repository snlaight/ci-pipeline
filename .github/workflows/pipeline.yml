name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Run one-liner
      run: echo "I'll be back."
    - name: Run two-liner
      run: |
        echo I'll be back,
        echo once again.